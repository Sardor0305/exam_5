package org.example.bot.util;


import org.example.bot.enam.*;

import java.util.HashMap;
import java.util.Map;

public class UtilLists {
    public static final Map<Long, CallDate> HOME_CHOICE_MAP = new HashMap<>();
    public static Map<Long, StateForBack> stateForBackMap = new HashMap<>();
    public static Map<Long, String> valueStateMap = new HashMap<>();
    public static Map<Long, String> valueValStateMap = new HashMap<>();
    public static final Map<Long, CalculateState> CALCULATE_STATE_MAP = new HashMap<>();
}
